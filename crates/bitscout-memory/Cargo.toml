[package]
name = "bitscout-memory"
version.workspace = true
edition.workspace = true

[dependencies]
bitscout-core = { path = "../bitscout-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
